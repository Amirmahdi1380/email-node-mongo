<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>صفحه اصلی</title>
    <style>
        body {
            font-family: 'Tahoma', sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #007bff;
        }

        .projects-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }

        .card {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 300px;
            margin-bottom: 20px;
        }

        .card h2 {
            color: #007bff;
            margin-bottom: 10px;
        }

        .card p {
            margin: 10px 0;
            color: #343a40;
        }
    </style>
</head>
<body>
    <h1>پروژه‌های بورسی</h1>
    <div class="projects-container">
        <% projects.forEach(project => { %>
            <div class="card">
                <h2><%= project.title %></h2>
                <p><%= project.description %></p>
                <p>حداقل سرمایه‌گذاری: <%= project.minInvest %></p>
                <p>سود مورد انتظار: <%= project.expectedProfit %></p>
            </div>
        <% }); %>
    </div>
</body>
</html>
